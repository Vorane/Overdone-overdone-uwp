<Page
    x:Class="overdone_uwp.Views.EditTaskView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:overdone_uwp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:uc="using:overdone_uwp.UserControls"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <uc:Header Title="Edit Task"/>
        <RelativePanel Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <StackPanel  x:Name="TaskForm">
                <TextBlock Text="Task Name"/>
                <TextBox x:Name="TaskNameTextBox"/>
                <TextBlock Text="Choose a Folder"/>
                <ComboBox x:Name="FolderComboBox"/>
                <DatePicker x:Name="TaskDeadline"/>
                <TimePicker x:Name="TaskRemindTime"/>
                <ToggleButton Content="IsRoutine"/>
            
            </StackPanel>


            <Button Content="Done" HorizontalAlignment="Stretch"   RelativePanel.AlignBottomWithPanel="True"/>
        </RelativePanel>

    </Grid>
</Page>

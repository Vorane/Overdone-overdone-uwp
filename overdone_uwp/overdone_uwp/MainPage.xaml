<Page
    x:Class="overdone_uwp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:overdone_uwp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <SplitView x:Name="rootSplitView" DisplayMode="Overlay">
        <SplitView.Pane>
            <StackPanel Margin="12">
                <Button x:Name="homeButton"  HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Content="Home"/>
                <ItemsControl x:Name="groupsItemControl" >
                    <ItemsControl.ItemTemplate>
                        <DataTemplate >
                            <StackPanel>
                                <ToggleButton x:Name="openGroup"  HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"  />
                                <ListView   SelectionMode="None" Margin="12,0,0,0" IsItemClickEnabled="True" >
                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="HorizontalAlignment" Value="Stretch" />
                                        </Style>
                                    </ListView.ItemContainerStyle>
                                    <ListView.ItemTemplate>
                                        <DataTemplate >
                                            <TextBlock  Style="{StaticResource BodyTextBlockStyle}" HorizontalAlignment="Stretch" />
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </SplitView.Pane>
        <Frame x:Name="rootFrame"/>
    </SplitView>
</Page>
